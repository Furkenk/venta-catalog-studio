const crypto=require('crypto');const {parseCookies}=require('./auth-utils.cjs');const {getUser}=require('./user-auth.js');
function session(v){const secret=process.env.SHOPIFY_SESSION_SECRET||process.env.SHOPIFY_API_SECRET;if(!secret)return null;const k=crypto.createHash('sha256').update(secret).digest();const[i,t,d]=String(v||'').split('.');if(!i||!t||!d)return null;try{const x=crypto.createDecipheriv('aes-256-gcm',k,Buffer.from(i,'base64url'));x.setAuthTag(Buffer.from(t,'base64url'));return JSON.parse(Buffer.concat([x.update(Buffer.from(d,'base64url')),x.final()]).toString('utf8'));}catch{return null;}}
function getSession(req){return session(parseCookies(req.headers.cookie||'').venta_shopify_session)}
async function graphql(s,q,variables={}){const r=await fetch(`https://${s.shop}/admin/api/2026-07/graphql.json`,{method:'POST',headers:{'Content-Type':'application/json','X-Shopify-Access-Token':s.accessToken},body:JSON.stringify({query:q,variables})});const text=await r.text();let j;try{j=JSON.parse(text)}catch{throw new Error(`Shopify API returned non-JSON (${r.status})`)}if(!r.ok||j.errors)throw new Error((j.errors||[]).map(e=>e.message).join('; ')||`Shopify API error (${r.status})`);return j.data}
module.exports=async(req,res)=>{try{if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});if(!getUser(req))return res.status(401).json({error:'Dahili kullanıcı girişi gerekli.'});const s=getSession(req);if(!s?.shop||!s?.accessToken)return res.status(401).json({error:'Shopify bağlantısı gerekli.'});const q=`query($after:String){collections(first:250,after:$after,sortKey:TITLE){nodes{id title handle descriptionHtml image{url}} pageInfo{hasNextPage endCursor}}}`;let after=null,items=[],hasNext=true;while(hasNext){const d=await graphql(s,q,{after});items.push(...d.collections.nodes.map(c=>({id:c.id,name:c.title,handle:c.handle,image:c.image?.url||null,description:c.descriptionHtml||''})));hasNext=d.collections.pageInfo.hasNextPage;after=d.collections.pageInfo.endCursor}return res.status(200).json({collections:items})}catch(e){console.error(e);return res.status(500).json({error:e.message||'Koleksiyonlar alınamadı.'})}};
