// Catalog Studio source is now authoritative. Legacy string patches were removed
// because they could silently overwrite the live layout implementation during build.
console.log('VENTA Catalog Studio: source implementation used as-is');
